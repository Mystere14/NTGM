struct enumeration
{
    currentNumber: i32,
    initialNumber: i32,
    addNumber: i32,
    endNumber:i32,
    symbolDefinition: char,
    isDifferentSteps: bool,
}

struct wordList
{
    symbolDefinition: char,
    list: array<string>,
    isDifferentSteps: bool,
}